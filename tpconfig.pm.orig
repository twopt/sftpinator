package tpconfig;

#Using tpconfig.pm to store global variables

use Exporter;
our (@EXPORT, @ISA);

@ISA = qw(Exporter);
# List variables to export
@EXPORT_OK = qw($SFTP_SERVER $SFTP_LOGIN $SFTP_PASSWD);

$SFTP_SERVER='sftp.twopoint.com';
$SFTP_LOGIN='';
$SFTP_PASSWD='';
